## What changed

<!-- One or two sentences. What does this PR do, and why now? -->

## Why this approach

<!-- Alternatives considered, and the tradeoff you accepted. Delete if trivial. -->

## Testing

- [ ] Unit tests added or updated
- [ ] `pnpm lint && pnpm typecheck && pnpm test` passes
- [ ] `python -m pytest` passes (if `apps/ai/` changed)
- [ ] Verified by hand in the browser

## Checklist

- [ ] No secrets, keys, or tokens in the diff
- [ ] New env vars documented in the relevant `.env.example`
- [ ] Migrations are additive and numbered in sequence
